{
	"name": "blog-example",
	"version": "1.0.0",
	"main": "server.ts",
	"type": "module",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"serve:deref": "node --experimental-strip-types serverDeref.ts | npx pino-pretty",
		"serve:ref": "node --experimental-strip-types serverRef.ts | npx pino-pretty"
	},
	"author": "jamee mikell (https://github.com/jmjf)",
	"license": "MIT",
	"description": "",
	"dependencies": {
		"@sinclair/typebox": "^0.34.30",
		"fastify": "^5.2.1"
	},
	"devDependencies": {
		"pino-pretty": "^13.0.0"
	}
}
